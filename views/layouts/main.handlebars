<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Problogger</title>
    <link rel="stylesheet" type="text/css" href="/css/jass.css" />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
  </head>
  <body>
    <header class="container-header">
      <h1>Problogger</h1>
      <h2>Blog + Commenting Platform</h2>
    </header>
    <nav class = 'nav'>
      <a href="/">Home</a>
      {{#if loggedIn}}
        <a href="/logout">Logout</a>
      {{else}}
        <a href="/login">Login</a>
      {{/if}}
    </nav>
    </main>
     <aside>
       <h3>Join the <a href="/conversation">Conversation</a></h3>
    </aside>
    <div class="blogPosts">
      {{{body}}}
    </div>
  <main>
      {{!-- {{#if loggedIn}}
        <script src="/logout"></script>
      {{/if}} --}}
  </body>

</html>